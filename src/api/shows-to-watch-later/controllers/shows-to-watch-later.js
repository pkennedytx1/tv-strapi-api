'use strict';

/**
 * shows-to-watch-later controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::shows-to-watch-later.shows-to-watch-later');
