'use strict';

/**
 * shows-to-watch-later router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::shows-to-watch-later.shows-to-watch-later');
